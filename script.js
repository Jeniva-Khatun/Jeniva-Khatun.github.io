const sections=document.querySelectorAll("section");const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")})},{threshold:.08});sections.forEach(section=>observer.observe(section));const form=document.querySelector(".contact-form");const message=document.querySelector(".form-message");form.addEventListener("submit",event=>{event.preventDefault();const name=form.querySelector('input[type="text"]').value.trim();const email=form.querySelector('input[type="email"]').value.trim();if(!name||!email){message.textContent="Please enter your name and email.";return}message.textContent=`Thanks, ${name}. Your message is ready to be sent.`;form.reset()});const menuButton=document.querySelector(".menu-toggle");menuButton.addEventListener("click",()=>{const nav=document.querySelector(".navbar nav");nav.classList.toggle("show-menu");if(nav.classList.contains("show-menu"))Object.assign(nav.style,{display:"flex",position:"absolute",top:"48px",left:"0",right:"0",padding:"20px",flexDirection:"column",background:"#e7e0d5",borderBottom:"1px solid rgba(0,0,0,0.2)"});else nav.removeAttribute("style")});document.querySelectorAll(".project").forEach(project=>{project.addEventListener("mouseenter",()=>project.style.transform="scale(0.985)");project.addEventListener("mouseleave",()=>project.style.transform="scale(1)")});